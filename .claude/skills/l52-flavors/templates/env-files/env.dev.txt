# Development Environment Configuration
#
# This file is loaded at build time via --dart-define-from-file=.env.dev
# Variables become compile-time constants accessible via String.fromEnvironment()
#
# NOTE: Debug/mock flags (useMockAuth, logNetworkRequests) belong in
# lib/core/constants/debug_constants.dart, not here.

# ===========================================================================
# FLAVOR IDENTIFICATION
# ===========================================================================

FLAVOR=dev
APP_NAME=My App (Dev)

# ===========================================================================
# API CONFIGURATION
# ===========================================================================

# Development API endpoint
API_URL=https://dev-api.example.com

# ===========================================================================
# SUPABASE (if using)
# ===========================================================================

# Development Supabase project
SUPABASE_URL=https://dev-project.supabase.co
SUPABASE_ANON_KEY=your-dev-anon-key

# ===========================================================================
# GOOGLE SIGN-IN (if using Social Login)
# ===========================================================================

# Web Client ID (same across environments unless using separate Google projects)
GOOGLE_WEB_CLIENT_ID=xxxxx.apps.googleusercontent.com

# iOS Client ID (same across environments)
GOOGLE_IOS_CLIENT_ID=xxxxx.apps.googleusercontent.com
